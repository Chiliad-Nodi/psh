#!/usr/local/sbin/pshs
import sys
print(sys.path)
Echo(['1']).chain(Printer()).call()
cat ("utils.py")
echo (1)
A = '5'
Echo(['Hello World']).chain(Printer()).call()
Echo([A]).chain(Printer()).call()

